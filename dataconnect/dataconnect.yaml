specVersion: "v1"
serviceId: "mobilecard"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mobilecard-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
